{% extends 'base-back.html.twig' %}
{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>content</th>
                <td>{{ post.content }}</td>
            </tr>
            
            <tr>
                <th>picture</th>
                <td>{{ post.image }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('list_posts') }}">Retour</a>

    <a href="{{ path('modifier_posts', {'id': post.id}) }}">Modifier</a>

    {{ include('back_office/posts/_supprimer_form.html.twig') }}
{% endblock %}
